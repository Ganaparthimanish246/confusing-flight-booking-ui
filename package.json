{
  "name": "confusing-flight-booking-ui",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "if [ ! -d \"flutter\" ]; then git clone -b stable https://github.com/flutter/flutter.git; fi && ./flutter/bin/flutter build web --release"
  }
}
